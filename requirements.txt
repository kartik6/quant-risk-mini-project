# Minimum versions, so the project installs on Python 3.10 and newer.
#
# Verified byte-identical output on both ends of this range:
#   pandas 2.2.2  / numpy 1.26.4 / scipy 1.13.1 / matplotlib 3.9.2  (Python 3.12)
#   pandas 3.0.5  / numpy 2.5.2  / scipy 1.18.1 / matplotlib 3.11.1 (Python 3.12)
#
# To pin the exact set this project was developed against, replace >= with ==
# and use the second row above.

yfinance>=0.2.40
pandas>=2.0
numpy>=1.24
scipy>=1.10
matplotlib>=3.7

# Optional, for the notebook (analysis.ipynb)
jupyter>=1.0
