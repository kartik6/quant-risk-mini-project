# Direct dependencies. Pinned so a fresh install reproduces the numbers.
# Python 3.12

yfinance==1.7.0
pandas==3.0.5
numpy==2.5.2
scipy==1.18.1
matplotlib==3.11.1

# Optional: for the notebook (analysis.ipynb)
jupyter>=1.0
